import { Carousel } from "./Carousel";
import { HeroCarousel } from "./HeroCarousel";

export const Hero = () => {
  return (
    <div className="w-full relative">
      {/* <HeroCarousel /> */}
      <Carousel />
    </div>
  );
};
